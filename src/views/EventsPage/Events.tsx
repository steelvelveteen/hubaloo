import React from 'react';

const Profile: React.FC = () => {
    const salutation = 'Events Page';

    return (
        <div>
            <div>{salutation}</div>
        </div>
    );
};

export default Profile;
