import React from 'react';

const Profile: React.FC = () => {
    const salutation = 'Member Profile';

    return (
        <div>
            <div>{salutation}</div>
        </div>
    );
};

export default Profile;
