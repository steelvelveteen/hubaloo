import React from 'react';

const Profile: React.FC = () => {
    const salutation = 'Webcams page';

    return (
        <div>
            <div>{salutation}</div>
        </div>
    );
};

export default Profile;
