import { makeStyles } from '@material-ui/core';

const welcomeTitleStyle = makeStyles(() => ({
    welcomeTitle: {
        textTransform: 'uppercase',
        letterSpacing: '0.5rem'
    }
}));

export default welcomeTitleStyle;
